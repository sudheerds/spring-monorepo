info:
  name: createOrder
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/orders/createOrder
  body:
    type: form-urlencoded
    data:
      - name: product
        value: Keyboard
      - name: price
        value: "150.0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Order-Service-Prometheus
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/api/actuator/prometheus
  auth: inherit


settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

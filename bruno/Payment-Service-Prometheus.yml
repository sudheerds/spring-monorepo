info:
  name: Payment-Service-Prometheus
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8081/api/actuator/prometheus
  auth: inherit


settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

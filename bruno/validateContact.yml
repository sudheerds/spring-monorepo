info:
  name: sendNotification
  type: http
  seq: 18

http:
  method: POST
  url: http://localhost:8083/api/notifications/sendNotification
  body:
    type: form-urlencoded
    data:
      - name: recipient
        value: alice@example.com
      - name: message
        value: "Hello, this is a test notification!"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: getNotifications
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8080/api/notifications/getNotifications
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

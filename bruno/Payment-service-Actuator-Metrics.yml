info:
  name: Payment-service-Actuator-Metrics
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8081/api/actuator/metrics
  auth: inherit


settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

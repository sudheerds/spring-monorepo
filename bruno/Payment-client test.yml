info:
  name: Payment-client test
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8080/api/orders/test-payment-call
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: paymentFail
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api/payments/fail
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

server:
  servlet:
    context-path: /api

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"

logging:
  pattern:
    level: "%5p [traceId=%X{traceId}]"

app:
  env: dev
  region: india
